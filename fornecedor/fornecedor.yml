spring:
  datasource:
    password: root
    url: "jdbc:postgresql://localhost:5432/fornecedor?useTimezone=true&serverTimezone=UTC"
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
